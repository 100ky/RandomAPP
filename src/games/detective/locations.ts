import { POILocation } from '../../types/game';

// Lokace pro únikovou hru Detektiva
export const detectiveLocations: POILocation[] = [
  // Zde budou v budoucnu definovány lokace pro detektivní hru
];
